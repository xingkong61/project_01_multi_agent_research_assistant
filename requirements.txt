# ===== 核心框架 =====
langgraph>=0.3.0
langchain>=1.0.0             # init_chat_model 工厂入口
langchain-core>=0.3.0
langchain-openai>=0.3.0

# ===== 研究工具 =====
langchain-tavily>=0.1.0      # 联网搜索（可选，不配置也能跑：arXiv + 网页抓取仍可用）
requests>=2.31.0             # arXiv API / 网页抓取
beautifulsoup4>=4.12.0       # 网页正文提取

# ===== 配置与测试 =====
python-dotenv>=1.0.0
pytest>=8.0.0

# ===== Web 界面（选项2：Streamlit 部署）=====
streamlit>=1.30.0            # app.py 的浏览器界面；仅跑 CLI 可不装

# ===== L2 生产服务（FastAPI + ASGI）=====
fastapi>=0.110.0             # web/server.py：SSE 流式研究接口 + 鉴权 + 静态托管
uvicorn[standard]>=0.27.0    # ASGI 服务器
